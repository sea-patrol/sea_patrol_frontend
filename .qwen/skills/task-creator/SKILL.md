---
name: task-creator
description: Создание новой задачи в бэклоге. Использовать при запросах создать задачу, тикет, TASK, добавить в бэклог, сгенерировать файл задачи по шаблону.
---

# Task Creator — Создание задач в бэклоге

## Назначение

Скилл автоматически создаёт новый файл задачи в папке `ai-docs/backlog/todo/` на основе шаблона `TASK-TEMPLATE.md`.

## Как это работает

1. **Инкремент номера**: Скрипт читает текущий номер из `.task-counter.json` и увеличивает его
2. **Генерация файла**: Создаётся файл `TASK-{N}.md` с подставленным ID и датой
3. **Сохранение**: Файл помещается в `ai-docs/backlog/todo/`

## Структура файлов

```
ai-docs/backlog/
├── .task-counter.json      # Служебный: следующий номер задачи
├── TASK-TEMPLATE.md        # Шаблон для копирования
└── todo/
    └── TASK-{N}.md         # Новые задачи
```

## Использование

### Через Qwen Code

Попросите создать задачу, например:
- "Создай новую задачу в бэклоге"
- "Добавь задачу TASK в todo"
- "Сгенерируй файл задачи по шаблону"

### Напрямую через CLI

```bash
node .qwen/skills/task-creator/scripts/create-task.js
```

## Пример вывода

```
✅ Создана задача: TASK-1.md
📁 Путь: C:\...\ai-docs\backlog\todo\TASK-1.md
🔢 ID: TASK-1
```

## Служебные файлы

| Файл | Назначение |
|------|------------|
| `.task-counter.json` | Хранит `nextId` — номер следующей задачи |
| `scripts/create-task.js` | Скрипт генерации на Node.js |

## Примечания

- Номер задачи инкрементируется автоматически
- Дата создания подставляется текущая (YYYY-MM-DD)
- Шаблон берётся из `ai-docs/backlog/TASK-TEMPLATE.md`
