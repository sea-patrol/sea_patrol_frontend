---
name: git-branch-creator
description: Создание Git-ветки для задачи. Использовать при запросах создать ветку, переключиться на задачу, начать работу над TASK.
---

# Git Branch Creator — Создание веток для задач

## Назначение

Скилл автоматически создаёт новую Git-ветку для задачи в формате `feature/TASK-{N}`, предварительно синхронизировавшись с master.

## Как это работает

1. **Проверка задачи**: Убеждается, что файл задачи существует в `todo/` или `done/`
2. **Проверка Git**: Проверяет чистоту working tree (нет незакоммиченных изменений)
3. **Git sync**:
   - `git fetch` — получение обновлений
   - `git checkout master` — переключение на master
   - `git pull --ff-only` — fast-forward merge (с fallback на `git reset --hard`)
4. **Создание ветки**: `git checkout -b feature/TASK-{N}`

## Структура файлов

```
.qwen/skills/git-branch-creator/
├── README.md                 # Документация
└── scripts/
    └── create-branch.js      # Скрипт на Node.js
```

## Использование

### Через Qwen Code

Попросите создать ветку, например:
- "Создай ветку для задачи TASK-1"
- "Создай feature/TASK-5"
- "Переключись на ветку для задачи 3"

### Напрямую через CLI

```bash
# С префиксом TASK-
node .qwen/skills/git-branch-creator/scripts/create-branch.js TASK-1

# Без префикса (только номер)
node .qwen/skills/git-branch-creator/scripts/create-branch.js 1
```

## Примеры вывода

### Успешное выполнение
```
🔍 Проверка задачи: TASK-1...
✅ Задача найдена в: ai-docs/backlog/todo

📊 Проверка состояния Git...
✅ Working tree чиста

📥 Синхронизация с удалённым репозиторием...
✅ Fetch завершён

🔄 Переключение на master...
✅ Переключено на master

⬇️  Pull с --ff-only...
✅ Pull завершён (fast-forward)

🌿 Создание ветки feature/TASK-1...
✅ Ветка создана: feature/TASK-1

==================================================
✅ Ветка успешно создана!
==================================================
📁 Путь к задаче: ai-docs/backlog/todo\TASK-1.md
🌿 Ветка: feature/TASK-1
📍 BASE: master (синхронизирован с origin)
```

### Ошибка: задача не найдена
```
❌ Задача не найдена: TASK-999
   Проверьте, что файл TASK-999.md существует в ai-docs/backlog/todo/ или ai-docs/backlog/done/
```

### Ошибка: working tree не чиста
```
❌ Working tree не чиста. Есть незакоммиченные изменения:
 M src/components/App.jsx
?? src/temp.txt

   Закоммитьте или отмените изменения перед созданием ветки.
```

### Ошибка: ветка уже существует
```
❌ Ветка уже существует: feature/TASK-1
   Удалите ветку или используйте другую.
```

### Ошибка: неверный формат
```
❌ Неверный формат задачи: abc
   Используйте формат: TASK-1 или 1
```

## Поддерживаемые форматы

| Ввод | Результат | Ветка |
|------|-----------|-------|
| `TASK-1` | `TASK-1` | `feature/TASK-1` |
| `TASK-01` | `TASK-1` | `feature/TASK-1` |
| `1` | `TASK-1` | `feature/TASK-1` |
| `01` | `TASK-1` | `feature/TASK-1` |

## Сценарии

| Сценарий | Поведение |
|----------|-----------|
| **Задача в todo/** | ✅ Ветка создаётся |
| **Задача в done/** | ✅ Ветка создаётся (для доработок) |
| **Задача не существует** | ❌ Ошибка |
| **Working tree не чиста** | ❌ Ошибка |
| **Ветка уже существует** | ❌ Ошибка |
| **Pull --ff-only не удался** | ⚠️ Reset на origin/master |

## Требования

- Node.js (рекомендуется последняя LTS-версия)
- Git (должен быть в PATH)
- Доступ к удалённому репозиторию (origin)

## Примечания

- Задача должна существовать в `ai-docs/backlog/todo/` или `ai-docs/backlog/done/`
- Working tree должна быть чистой (нет незакоммиченных изменений)
- При неудаче `git pull --ff-only` выполняется `git reset --hard origin/master`
- Ветка всегда создаётся от актуального master
