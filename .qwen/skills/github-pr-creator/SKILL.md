---
name: github-pr-creator
description: Создание Pull Request на GitHub. Использовать при завершении разработки фичи, готовности к code review, перед мерджем.
---

# GitHub PR Creator — Создание Pull Request

## Назначение

Скилл автоматически создаёт Pull Request на GitHub для задачи, предварительно закоммитив изменения и запушив ветку.

## Как это работает

1. **Проверка задачи**: Убеждается, что файл задачи существует в `todo/` или `done/`
2. **Проверка ветки**: Проверяет, что вы находитесь на ветке `feature/TASK-{N}`
3. **Git commit**: Автоматически коммитит все изменения (`git add . && git commit`)
4. **Git push**: Запушивает ветку на GitHub (`git push -u origin feature/TASK-{N}`)
5. **GitHub CLI**:
   - Если установлен: создаёт PR через `gh pr create`
   - Если не установлен: выводит ссылку для создания PR через веб-интерфейс

## Структура файлов

```
.qwen/skills/github-pr-creator/
├── SKILL.md                # Документация скилла
└── scripts/
    └── create-pr.js        # Скрипт на Node.js
```

## Использование

### Через Qwen Code

Попросите создать PR, например:
- "Создай Pull Request для задачи TASK-1"
- "Запушить ветку и создать PR"
- "Создай PR на GitHub"

### Напрямую через CLI

```bash
# С префиксом TASK-
node .qwen/skills/github-pr-creator/scripts/create-pr.js TASK-1

# Без префикса (только номер)
node .qwen/skills/github-pr-creator/scripts/create-pr.js 1
```

## Примеры вывода

### Успешное выполнение (с GitHub CLI)
```
🔍 Проверка задачи: TASK-1...
✅ Задача найдена в: ai-docs/backlog/todo

📊 Проверка текущей ветки...
✅ Текущая ветка: feature/TASK-1

📊 Проверка состояния Git...
✅ Working tree чиста

📊 Проверка истории коммитов в ветке...
✅ Есть коммиты для публикации

🚀 Push ветки на GitHub...
✅ Ветка запушена: origin/feature/TASK-1

🔍 Проверка GitHub CLI...
✅ GitHub CLI доступен

📝 Создание Pull Request...

==================================================
✅ Pull Request успешно создан!
==================================================
📝 PR: TASK-1: Добавление новой фичи
🌿 Ветка: feature/TASK-1
📎 URL: https://github.com/ivvko77/sea_patrol_frontend/pull/1
```

### Успешное выполнение (без GitHub CLI)
```
...
🔍 Проверка GitHub CLI...
⚠️  GitHub CLI не найден.

📋 Для создания PR используйте веб-интерфейс:
   https://github.com/ivvko77/sea_patrol_frontend/compare/feature/TASK-1?expand=1

📝 Рекомендуемое название PR:
   TASK-1: Добавление новой фичи
```

### Ошибка: не та ветка
```
❌ Вы находитесь в ветке: master
   Переключитесь на ветку feature/TASK-1 перед созданием PR.
   Используйте: git checkout feature/TASK-1
```

### Ошибка: задача не найдена
```
❌ Задача не найдена: TASK-999
   Проверьте, что файл TASK-999.md существует в ai-docs/backlog/todo/ или ai-docs/backlog/done/
```

### Ошибка: нет коммитов
```
⚠️  В ветке нет новых коммитов.
   Внесите изменения и закоммитьте их перед созданием PR.
```

### Ошибка: PR уже существует
```
⚠️  Pull Request уже существует!
🌿 Ветка: feature/TASK-1

📋 Откройте PR в веб-интерфейсе:
   https://github.com/ivvko77/sea_patrol_frontend/pulls
```

## Поддерживаемые форматы

| Ввод | Результат | Ветка |
|------|-----------|-------|
| `TASK-1` | `TASK-1` | `feature/TASK-1` |
| `TASK-01` | `TASK-1` | `feature/TASK-1` |
| `1` | `TASK-1` | `feature/TASK-1` |
| `01` | `TASK-1` | `feature/TASK-1` |

## Сценарии

| Сценарий | Поведение |
|----------|-----------|
| **Есть изменения** | ✅ Автоматический коммит |
| **Нет изменений** | ✅ Продолжение (если есть коммиты) |
| **Не та ветка** | ❌ Ошибка |
| **Нет коммитов** | ❌ Ошибка |
| **GitHub CLI установлен** | ✅ PR через `gh pr create` |
| **GitHub CLI не установлен** | ⚠️ Ссылка на веб-интерфейс |
| **PR уже существует** | ⚠️ Предупреждение |

## Требования

- Node.js (рекомендуется последняя LTS-версия)
- Git (должен быть в PATH)
- GitHub CLI (`gh`) — опционально, но рекомендуется
- Доступ к удалённому репозиторию (origin)
- Ветка должна быть запушена

## Установка GitHub CLI

### Windows
```bash
winget install GitHub.cli
```

### macOS
```bash
brew install gh
```

### Linux
```bash
sudo apt install gh  # Debian/Ubuntu
```

### Аутентификация
```bash
gh auth login
```

## Формат Pull Request

Скилл создаёт PR со следующим содержимым:

**Заголовок**: `{TASK-ID}: {Заголовок задачи}`

**Тело PR**:
```markdown
## Описание
Реализация задачи TASK-1

## Изменения
- [ ] Добавить описание изменений

## Checklist
- [ ] `npm run build` - сборка прошла успешно
- [ ] `npm run lint` - линтинг пройден
- [ ] Тесты пройдены

## Скриншоты (если применимо)
<!-- Добавьте скриншоты изменений -->

## Related Issues
Closes #1
```

## Примечания

- Задача должна существовать в `ai-docs/backlog/todo/` или `ai-docs/backlog/done/`
- Вы должны находиться на ветке `feature/TASK-{N}`
- Все изменения автоматически коммитятся с сообщением `feat: изменения по задаче {TASK-ID}`
- PR создаётся с базой `master`
- Временный файл `.pr-body.md` удаляется после создания PR
